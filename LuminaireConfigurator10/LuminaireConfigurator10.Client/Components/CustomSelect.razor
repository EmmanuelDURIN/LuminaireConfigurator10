@typeparam TItem
@typeparam TValue
@typeparam TDisplay


<select class="form-control">
    @ChildContent
    @foreach (var item in Items)
    {
        <option value="@ValueSelector(item)">@DisplaySelector(item)</option>
    }

</select>
