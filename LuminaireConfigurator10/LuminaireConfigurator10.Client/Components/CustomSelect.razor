@typeparam TItem
@typeparam TValue
@typeparam TDisplay

<select class="form-control" @bind="@SelectedValue" size="@Size">
    @ChildContent
    @if (Items!= null)    
    {
        foreach (var item in Items)
        {
            bool isSelected = item?.Equals(Selected) == true;
            <option value="@ValueSelector(item)" selected="@isSelected">@DisplaySelector(item)</option>
        }
    }
</select>